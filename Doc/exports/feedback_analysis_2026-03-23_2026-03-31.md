# Анализ замечаний по выгрузкам (2026-03-23 .. 2026-03-31)

Источник: 
- `/Volumes/SSD APFS/Python Project/RoleModel_helper/Doc/exports/all_chats_with_feedback_87.242.86.193_2026-03-23_2026-03-31.md`
- дубликаты проверены по файлам `dialogues_2026-03-24_2026-03-25.md`, `dialogues_with_feedback_87.242.86.193_2026-03-23_2026-03-27.md`.

Всего сессий с `DOWN`: **10** (уникальные по `session_id`).

## Классификация

### A. Корректно сформулировано + есть ожидаемое действие (можно брать в работу сразу)
1. `44ba004d-2fa5-4511-9730-e30a329dfda6`
- Суть: в инструкции искажается название АС; дана инструкция «по требованию», хотя роль в ответе была «по умолчанию».
- Почему корректно: есть конкретный симптом и ожидаемое поведение.
- Что править: привязать instruction path к `access_level` из structured слоя и запретить fallback в `по требованию`, если по факту `level=1`.
- Явная ошибка роутинга: **да** (неверный путь/шаблон инструкции для текущего контекста).

2. `8a8c5c21-1b82-44af-bc7c-e90a154edb01`
- Суть: неподходящие отделы для города + неполный набор профилей.
- Почему корректно: описаны 2 проверяемые проблемы в фильтрации/ранжировании.
- Что править: строгий фильтр `город + отдел + должность`, затем ранжирование профилей внутри отфильтрованного множества.
- Явная ошибка роутинга: **частично да** (после «Нет профиля» сценарий сбросился и ушел в повторный сбор слотов вместо корректного recovery).

3. `b19e19ee-c37e-4311-9583-3c692ac860ba`
- Суть: пользователь просит общий список АС, агент циклически требует конкретную АС.
- Почему корректно: ожидаемое поведение явно сформулировано (выдать перечень и пояснение).
- Что править: broad discovery path (`BROWSE_SYSTEMS`) с пагинацией и без обязательного `resolved_system_id` на старте.
- Явная ошибка роутинга: **да** (неверная маршрутизация broad intent в strict resolve).

4. `ec952cd4-6a37-467a-a873-55662ece1b27`
- Суть: пользователь прямо говорит, что не знает систему; агент отвечает «не удалось распознать АС».
- Почему корректно: понятная боль, ожидаем fallback-режим.
- Что править: при `unknown system` переключение в режим подбора/списка АС, а не hard-fail.
- Явная ошибка роутинга: **да**.

5. `12989909-2cd3-4ae5-927e-d3d698225ba5`
- Суть: для Wi‑Fi доступа агент сразу дает «по требованию», без проверки профиля/режима.
- Почему корректно: указан ожидаемый порядок (уточнить роль/профиль, проверить автоматически vs по требованию).
- Что править: сначала structured check (`profile + system + entitlement/access_level`), потом только соответствующая инструкция.
- Явная ошибка роутинга: **да**.

6. `1b1fe122-ac52-4c17-9d5d-793051bb1716`
- Суть: пользователь не знает профиль, агент зацикливается на «выберите профиль».
- Почему корректно: наблюдаемый loop и явный пробел UX.
- Что править: fallback «не знаю профиль» -> auto-recommend top profile + объяснение, либо уточнение минимальными вопросами.
- Явная ошибка роутинга: **да** (зависание в pending confirmation).

### B. Корректно по сути, но ожидание неполное/частично противоречивое (нужна формализация правила)
1. `48e9ad28-7aa1-4338-9d8f-a70d0ded443f`
- Суть: пользователь ожидает немедленную выдачу ролей на шаге выбора профиля.
- Проблема формулировки: не зафиксирован критерий, когда можно авто-выбрать профиль без подтверждения.
- Что доформализовать: порог confidence для auto-select и policy «если профилей >1, спрашиваем подтверждение / показываем next page».
- Явная ошибка роутинга: **частично** (фраза «Тут нет моего профиля» должна запускать профильный browse/re-rank, а не просто “следующие варианты” без контекстного объяснения).

2. `e6dfb99d-a3b4-4aa3-a5d5-f48b734c5854`
- Суть: «рекомендация профиля не относящегося к сотруднику».
- Проблема формулировки: нет точного ожидаемого профиля/критерия несоответствия в комментарии.
- Что доформализовать: правило валидации профиля против city/department/position и объяснение score в ответе.
- Явная ошибка роутинга: **скорее нет**, это больше качество matching/ranking и explainability.

### C. UI/формат ответа (не routing, но полезно)
1. `137a1c53-ac2d-4e62-ac9b-90e45d13e43f`
- Суть: просит не показывать «Сегмент», только роли.
- Что править: отдельный user-facing форматтер для ROLE_DISCOVERY (только бизнес-роли + type/access), служебные элементы скрыть.
- Явная ошибка роутинга: **нет**.

### D. Слабое/недостаточно конкретное замечание
1. `a5c56bfc-dfa3-456b-a064-adf7e59d985a`
- Комментарий: «Нет 7 отдела».
- Почему слабое: не указан ожидаемый шаг в UI/диалоге.
- Как сделать рабочим требованием: «если вводится числовой shorthand (`7`), показать 5 ближайших вариантов отделов и попросить выбрать».
- Явная ошибка роутинга: **нет** (это input normalization + candidate suggestion).

## Где явно ошибки роутинга (сводно)
Явный routing/policy дефект наблюдается в сессиях:
- `1b1fe122-ac52-4c17-9d5d-793051bb1716` (loop в profile confirmation)
- `44ba004d-2fa5-4511-9730-e30a329dfda6` (неверный instruction path: "по требованию" вместо "автоматически")
- `b19e19ee-c37e-4311-9583-3c692ac860ba` (broad intent ошибочно отправляется в strict system resolve)
- `ec952cd4-6a37-467a-a873-55662ece1b27` (unknown system -> hard fail вместо browse)
- `12989909-2cd3-4ae5-927e-d3d698225ba5` (преждевременный уход в instruction, минуя structured проверку)
- `8a8c5c21-1b82-44af-bc7c-e90a154edb01` (после «Нет профиля» некорректный сценарный reset)

## Приоритет доработок
P0 (сразу):
- Routing guardrails для broad/unknown system и profile confirmation loop.
- Жесткая связка instruction-ответа с `access_level` (`1 -> Автоматически`, `2 -> По требованию`).

P1:
- Улучшение profile matching/filtering по `город + отдел + должность` + explainability score.
- Recovery-переходы после отрицания кандидата (`нет моего профиля`, `не то`).

P2:
- Формат ответа (скрыть служебные сегменты в пользовательском представлении).
- Обработка shorthand ввода (`7`, `отдел 1`) через candidate suggestions.

## Дополнительная классификация (предложение)
Для следующих выгрузок удобно сразу маркировать каждый feedback этими тегами:
- `ROUTING`
- `MATCHING_RANKING`
- `DATA_SCOPE_FILTER`
- `ANSWER_FORMAT`
- `INSTRUCTION_GROUNDING`
- `INPUT_NORMALIZATION`
- `EXPECTATION_INCOMPLETE`

Это ускорит triage и план фиксов по приоритетам.
